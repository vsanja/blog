#ekvivalentno ovom dole ali napisano u htmlu
#<form action="/user/<%= @user.id %>" method="post">
  # <input type="hidden" name="_token" value="<%= request_forgery_protection_token %>" />

  #<div class="field">
  # <label for="user_name">Name</label><br>
 #  <input type="text" name="user[name]" id="user_name" value="<%= @user.name %>" />
 # </div>
# </form>


<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.password_field :password %>
  </div>
    <div class="field">
      <%= f.label :email %><br>
      <%= f.text_field :email %>
    </div>
  <div class="field">
    <%= f.label :password_confirmation %><br>
    <%= f.password_field :password_confirmation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
